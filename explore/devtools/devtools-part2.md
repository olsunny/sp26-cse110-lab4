1. The bug is that both num1 and num2 are string expressions so the result is also of string type, making the sum calculation incorrect since the program is 
only concatenating the strings
2. I would fix it by parsing the string into a number.